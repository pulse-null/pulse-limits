[package]
name = "pulse-tui"
version = "0.1.0"
edition = "2021"
description = "PulseLimits in the terminal: the Claude plan-limits monitor as a ratatui screen"
license = "AGPL-3.0-or-later"

[dependencies]
crossterm = "0.29.0"
ratatui = "0.30.2"
serde_json = "1.0.151"

[profile.release]
strip = true
lto = "thin"
