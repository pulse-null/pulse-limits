---
name: Bug report
about: Something shows the wrong thing, or nothing
labels: bug
---

**Version and platform**: output of `pulse-limits version`; macOS or Linux, which bar (SwiftBar, Waybar, none).

**Provider**: grok, claude or codex, and the plan you are on.

**What you expected**, and what you saw instead.

**`pulse-limits doctor` output** (it prints no tokens; it does print file paths and, for some providers, account ids, so look before pasting):

```
paste here
```

**If a number is wrong**, `pulse-limits raw <provider>` with identifiers redacted:

```
paste here
```
